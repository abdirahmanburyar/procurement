apiVersion: apps/v1
kind: Deployment
metadata:
  name: purchase-order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: purchase-order-service
  template:
    metadata:
      labels:
        app: purchase-order-service
    spec:
      imagePullSecrets:
      - name: ghcr-image-pull-secret
      containers:
      - name: purchase-order-service
        image: ghcr.io/abdirahmanburyar/procurement-purchase-order-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8085
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "docker"
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://$(POSTGRES_HOST):$(POSTGRES_PORT)/procurement_po_db"
        - name: POSTGRES_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: postgres-host
        - name: POSTGRES_PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: postgres-port
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: postgres-user
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: po-db-password
        - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: eureka-url
        - name: JAVA_OPTS
          value: "-Xmx512m -Xms256m"
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8085
          initialDelaySeconds: 120
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8085
          initialDelaySeconds: 90
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: purchase-order-service
spec:
  selector:
    app: purchase-order-service
  ports:
  - port: 8085
    targetPort: 8085
  type: ClusterIP

